<div class="inspection-map">
	{{#each maps}}
	<img src="{{this.thumbnail}}" data-map="{{this.name}}" id="{{this.name}}-cam" class="map-figure {{this.pass}}">
	{{/each}}
	<div class="arrow"></div>
</div>

<div class="canvas-map">
	<div class="padder">
		<div class="graphic"></div>
	</div>
</div>
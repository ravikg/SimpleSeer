{{#if header}}
  {{{header}}}
{{/if}}
<div class="{{classes}}">
  <div class="thead">
    {{#each cols}}
      <div class="th {{#if this.sortable}}sortable{{/if}}" title="{{this.title}}" data-key="{{this.key}}">
        {{this.title}}
      </div>
    {{/each}}
  </div>
  <div class="tcontent">
    <div class="tbody">
      <div class="tscroll infs-wrapper">
        {{#each rows}}
          {{{this}}}
        {{/each}}
      </div>
    </div>
  </div>
</div>

---
cameras:
  - id: -1
    name: Default Camera

record_all: 1
poll_rate: 0.5

in_cloud : 0

web: 
  address: localhost:8080
  static: ${static}

mongo: 
  host: 127.0.0.1
  port: 27017

database: ${project}

pub_uri: ipc:///tmp/${project}-pub
sub_uri: ipc:///tmp/${project}-sub


# To enable login just uncomment the following lines
# you will also need to add @login_required decorator
# to the view you want to force login for, see views.py
#login:
  #username: 'seer'
  #password: 'seer'

---
cameras:
  - id: 0
    name: Default Camera

record_all: 1
max_frames: 100

poll_rate: 0.5

web: 
  address: localhost:8080
  static: ${static}

mongo: 
  host: 127.0.0.1
  port: 27017

database: ${project}

pub_uri: ipc:///tmp/${project}-pub
sub_uri: ipc:///tmp/${project}-sub
